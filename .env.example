# Simple LM Agent Configuration
# Copy this file to .env and update the values

# REQUIRED: Path to your model file (GGUF or SafeTensors format)
# Examples:
# Windows GGUF: MODEL_PATH=C:\path\to\your\model.gguf
# Windows SafeTensors: MODEL_PATH=C:\path\to\your\model.safetensors
# Linux/Mac GGUF: MODEL_PATH=/path/to/your/model.gguf
# Linux/Mac SafeTensors: MODEL_PATH=/path/to/your/model.safetensors
# LM Studio: MODEL_PATH=C:\Users\YourName\.cache\lmstudio\models\organization\model-name\model.gguf
MODEL_PATH=path/to/your/model.gguf
TOKENIZER_PATH=path/to/your/tokenizer.json
# OPTIONAL: Path to tokenizer file (if not in same folder as model)
# The app will first search for tokenizer.json in the same folder as your model.
# Only set this if you need to specify a different location.
# Examples:
# Windows: TOKENIZER_PATH=C:\path\to\tokenizer.json
# Linux/Mac: TOKENIZER_PATH=/path/to/tokenizer.json
# TOKENIZER_PATH=path/to/your/tokenizer.json

# Optional: You can add more configuration variables here in the future
# CONTEXT_SIZE=4096
# MAX_TOKENS=512
# TEMPERATURE=0.7